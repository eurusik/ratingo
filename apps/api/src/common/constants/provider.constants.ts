/**
 * Provider System Constants
 *
 * Shared constants for provider-related functionality.
 */

/** Default priority for providers when not specified */
export const DEFAULT_PROVIDER_PRIORITY = 100;
